/// \ brief recibe el  precio original y el  porcentaje ,aplica el descuento y lo devuelve
/// \ param el precio original
/// \ param el porcentaje
/// \ return el nuevo precio con el descuento
float CalcularDescuento(float,int);

/// \ brief recibe el  precio original y el porcentaje,aplica el interes y lo devuelve
/// \ param el precio original
/// \ param el porcentaje
/// \ return el nuevo precio con el interes
float CalcularInteres(float,int);

/// \ brief recibe 2 numeros , los divide  y devuelve el resultado
/// \ param el numero que  hay que  dividir(dividendo)
/// \ param el numero que  divide al dividendo
/// \ return el  resultado de la  divicion
float Dividir(float,float);

/// \ brief recibe el  monto  en la moneda local(pesos) lo  pasa a Bitcoin y lo devuelve
/// \ param el monto en la moneda local
/// \ return el  resultado de la conversion
float PesosABitcoin(float);

/// \ brief recibe 2 numeros, los resta y devuelve el resultado
/// \ param primer  numero
/// \ param segundo numero
/// \ return el  resultado de la  resta
float Restar(float,float);

